spring:
  application:
    name: CatlogEntryEurekaClientLiberty
  cloud:
    loadbalancer:
      retry:
        enabled: true  
      
      
eureka:
  client:
    service-url:
      defaultZone: http://localhost:5000/eureka/

  
      